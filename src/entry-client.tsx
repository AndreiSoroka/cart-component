import createReactApplication from "@/app/createReactApplication";
import getRootElement from "@/app/getRootElement";
import getApplication from "@/app/getApplication";

createReactApplication(getRootElement(), getApplication());
